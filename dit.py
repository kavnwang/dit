import torch
import torch.nn as nn

class DiT (nn.Module):
    def __init__(
        self,
        num_blocks: int = 12,
        hidden_size: int = 384, 
        heads: int = 6,

    ):
        super().__init__()
        
    
    def forward(self, )
